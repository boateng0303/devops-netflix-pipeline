apiVersion: apps/v1
kind: Deployment
metadata:
  name: netflix-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: netflix
  template:
    metadata:
      labels:
        app: netflix
    spec:
      containers:
      - name: netflix
        image:  IMAGE_URI_PLACEHOLDER
        ports:
        - containerPort: 80
      imagePullSecrets:
      - name: regcred
